{% extends 'base.html.twig' %}

{% block title %}
    New Project
{% endblock %}

{%block stylesheets%}
    <link rel="stylesheet" href="{{asset('styles/home.css')}}">
{%endblock%}

{% block javascripts %}
    {{ parent() }}
    {{ importmap(['app', 'mangaPage']) }}
{% endblock %}

{% block body %}
    {{form_start(form, {'attr' : {'data-turbo' : 'false'} } )}}
    <section class="home-section">
        <div class="new-project-left">
            <h1>Nouveau Manga :</h1>
            <div class="new-project-top-left">
                <span class="span-color">Titre du Manga :</span>
                {{ form_widget(form.title, {'attr' : {'placeholder' : 'Titre du manga'} }) }}

                <span class="span-color">Sens de Lecture :</span>
                <div class="reading-direction">
                    {% for child in form.readingDirection %}
                        <label class="reading-direction-btn">
                            {{ form_widget(child) }}
                            <span class="btn-text">{{ child.vars.label }}</span>
                        </label>
                    {% endfor %}
                </div>
            </div>
            <span  class="span-color, margin-bottom-span">Ajouter des pages :</span>
            <div id="upload-pages" class="upload-pages">
                <div class="new-project-middle">
                    <span class="delete-btn-pages" style="display: none">&times;</span>
                    {{ form_widget(form.firstChapter.pagesFiles, {
                    'id': 'file-upload',
                    'attr': {
                    'multiple' : 'multiple',
                    'class': 'file-input',
                    'accept': 'image/png, image/jpeg'
                    }
                    }) }}
                    <label for="file-upload" class="upload-file">
                        <span class="plus-icon">+</span>
                        <img class="image-preview" src="" alt="Aperçu" style="display: none;">
                    </label>
                </div>
            </div>
            <span class="span-color">Synopsis :</span>
            {{ form_widget(form.synopsis, {'attr': {'class' : 'project-synopsis', 'placeholder' : 'Synopsis'} }) }}
        </div>
        <div class="new-project-right">
            <span class="span-color">Titre du chapître :</span>
            {{ form_widget(form.firstChapter.title, {'attr' : {'placeholder' : 'Titre du chapitre 1'} }) }}
            <span  class="span-color">Miniature :</span>
            <div id="upload-miniature" class="upload-miniature-container">
                <div class="new-project-middle">
                    <span class="delete-btn-pages" style="display: none">&times;</span>

                    {{ form_widget(form.miniatureFile, {
                    'id': 'file-miniature',
                    'attr': {
                    'class': 'file-input-miniature',
                    'accept': 'image/png, image/jpeg'
                    }
                    }) }}

                    <label for="file-miniature" class="upload-file">
                        <span class="plus-icon">+</span>
                        <img class="image-preview" src="" alt="Aperçu" style="display: none;">
                    </label>
                </div>
            </div>
            <span  class="span-color margin-top-class">Catégories :</span>
            <div class="categories-section">
                {% for child in form.categories %}
                    <label class="category-btn">
                        {{ form_widget(child) }}
                        <span class="btn-text">{{ child.vars.label }}</span>
                    </label>
                {% endfor %}
            </div>
            <span class="span-color">Statut :</span>
            <div class="status-section">
                {% for child in form.status %}
                    <label class="status-btn">
                        {{ form_widget(child) }}
                        <span class="btn-text">{{ child.vars.label }}</span>
                    </label>
                {% endfor %}
            </div>
            <span class="span-color">Publication :</span>
            <div class="publish-section">
                <label class="publish-btn">
                    {% for child in form.firstChapter.published %}
                        {{ form_widget(child) }}
                        <span class="btn-text"> {{child.vars.label}} </span>
                    {% endfor %}
                </div>
                <div class="userpage-buttons-save">
                    <button type="submit">Créer le manga</button>
                    <button type="button" onclick="window.history.back();">Annuler</button>
                </div>
            </div>
        </section>
        {{ form_end(form) }}
    {% endblock %}
