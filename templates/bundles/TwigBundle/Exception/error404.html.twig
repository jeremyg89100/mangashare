<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page introuvable — MangaShare</title>
    <link rel="icon" href="{{ asset('favicon.ico') }}" sizes="any">
    <style>
        :root { color-scheme: dark; }
        body {
            margin: 0; min-height: 100vh; display: flex; align-items: center; justify-content: center;
            background: #111827; color: #f3f4f6;
            font-family: 'Segoe UI', system-ui, Arial, sans-serif; text-align: center; padding: 1.5rem;
        }
        .error-card { max-width: 32rem; }
        .error-code { font-size: 6rem; font-weight: 800; color: #f97316; line-height: 1; margin: 0; }
        .error-title { font-size: 1.6rem; margin: 0.5rem 0 1rem; }
        .error-text { color: #9ca3af; margin-bottom: 2rem; }
        .error-link {
            display: inline-block; padding: 0.75rem 1.75rem; border-radius: 0.5rem;
            background: #f97316; color: #111827; font-weight: 700; text-decoration: none;
            transition: background 0.2s;
        }
        .error-link:hover { background: #fb923c; }
    </style>
</head>
<body>
    <div class="error-card">
        <p class="error-code">404</p>
        <h1 class="error-title">Page introuvable</h1>
        <p class="error-text">Désolé, la page que vous recherchez n'existe pas ou a été déplacée.</p>
        <a class="error-link" href="{{ path('app_home') }}">Retour à l'accueil</a>
    </div>
</body>
</html>
